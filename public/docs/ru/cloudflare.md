# Менеджер Cloudflare DNS

Управляйте записями Cloudflare DNS прямо из Marix.

## Интерфейс менеджера Cloudflare DNS

![Интерфейс менеджера Cloudflare DNS](/cf.png)

## Настройка

### Шаг 1: Получение API-токена

1. Перейдите в [Панель управления Cloudflare](https://dash.cloudflare.com)
2. Перейдите в Профиль → API-токены
3. Нажмите «Создать токен»
4. Используйте шаблон «Edit zone DNS» или создайте собственный с:
   - Zone.DNS: Редактирование
   - Zone.Zone: Чтение
5. Скопируйте токен

### Шаг 2: Настройка в Marix

1. Перейдите в **Настройки** в боковой панели
2. Найдите раздел **«API-токен Cloudflare»**
3. Нажмите **«Добавить токен»**
4. Вставьте ваш токен
5. Нажмите **«Сохранить»**

## Использование Cloudflare DNS

1. Нажмите **«Cloudflare»** в боковой панели
2. Ваши домены загрузятся в выпадающем списке
3. Выберите домен
4. Появятся DNS-записи для этого домена

## Просмотр DNS-записей

Таблица показывает:

| Столбец | Описание |
|---------|----------|
| Тип | Тип записи (A, AAAA, CNAME, MX, TXT и т.д.) |
| Имя | Имя записи/поддомен |
| Содержимое | Значение записи (IP, имя хоста, текст) |
| TTL | Время жизни (Авто или секунды) |
| Прокси | Через Cloudflare или только DNS |

## Добавление DNS-записи

### Шаг 1: Открытие диалога
Нажмите кнопку **«Добавить запись»**

### Шаг 2: Заполните данные

| Поле | Описание |
|------|----------|
| Тип | Выберите: A, AAAA, CNAME, MX, TXT, SRV и т.д. |
| Имя | Поддомен (например, «www») или «@» для корня |
| Содержимое | Значение (IP для A, имя хоста для CNAME и т.д.) |
| TTL | Время жизни: Авто или секунды |
| Прокси | Переключатель: Проксирование (оранжевое облако) или только DNS (серое) |
| Комментарий | Необязательная заметка |

### Шаг 3: Создание
Нажмите **«Создать»**

## Распространённые типы записей

### Запись A
Указывает домен на IPv4-адрес.

| Поле | Пример |
|------|--------|
| Тип | A |
| Имя | @ (или www) |
| Содержимое | 192.168.1.100 |

### Запись AAAA
Указывает домен на IPv6-адрес.

| Поле | Пример |
|------|--------|
| Тип | AAAA |
| Имя | @ |
| Содержимое | 2001:db8::1 |

### Запись CNAME
Создаёт псевдоним для другого домена.

| Поле | Пример |
|------|--------|
| Тип | CNAME |
| Имя | www |
| Содержимое | example.com |

### Запись MX
Указывает почтовые серверы.

| Поле | Пример |
|------|--------|
| Тип | MX |
| Имя | @ |
| Содержимое | mail.example.com |
| Приоритет | 10 |

### Запись TXT
Хранит текстовые данные (SPF, DKIM, верификация).

| Поле | Пример |
|------|--------|
| Тип | TXT |
| Имя | @ |
| Содержимое | v=spf1 include:_spf.google.com ~all |

## Редактирование DNS-записи

1. Найдите запись в таблице
2. Нажмите кнопку **Редактировать** (значок карандаша)
3. Измените поля
4. Нажмите **«Сохранить»**

## Удаление DNS-записи

1. Найдите запись
2. Нажмите кнопку **Удалить** (значок корзины)
3. Подтвердите удаление

## Статус прокси

| Значок | Статус | Эффект |
|--------|--------|--------|
| 🟠 Оранжевое облако | Проксируется | Трафик идёт через Cloudflare CDN |
| ⚪ Серое облако | Только DNS | Прямое подключение к источнику |

### Когда использовать прокси

**Проксирование (оранжевое):**
- Веб-серверы (HTTP/HTTPS)
- Нужна защита от DDoS
- Нужно кэширование CDN

**Только DNS (серое):**
- SSH, FTP, почтовые серверы
- Не-HTTP сервисы
- Нужно прямое IP-подключение

## Обновление записей

Нажмите кнопку **Обновить** рядом с выпадающим списком доменов для перезагрузки записей.

## Советы

- **TTL Авто** рекомендуется для большинства случаев
- **Тестируйте изменения** с помощью инструментов DNS-запросов
- **Делайте резервные копии записей** перед крупными изменениями
- **Распространение** может занять до 48 часов по всему миру
