# SFTP файловый менеджер

SFTP (SSH File Transfer Protocol) обеспечивает безопасную передачу файлов через зашифрованное SSH-подключение.

## Открытие SFTP

### Из существующего SSH-подключения

1. Подключитесь к SSH-серверу
2. Нажмите кнопку **SFTP** вверху сессии
3. Или используйте горячую клавишу: `Ctrl+Shift+O`

### Прямое SFTP-подключение

1. Щёлкните правой кнопкой на SSH-сервере в списке хостов
2. Выберите **«SFTP»** из контекстного меню

## Макет интерфейса

![Интерфейс SFTP](/sftp.png)

## Элементы управления навигацией

| Кнопка | Функция |
|--------|---------|
| ← | Назад в истории |
| → | Вперёд в истории |
| ↑ | К родительской директории |
| 🏠 | К домашней директории |
| 🔄 | Обновить текущую директорию |
| 🔍 | Поиск/фильтр файлов |
| 💾 | Выбор диска (Windows/Linux) |

## Файловые операции

### Скачивание файла (Удалённый → Локальный)

1. Перейдите к файлу на панели **УДАЛЁННЫЙ** (справа)

**Способ A: Перетаскивание**
- Нажмите и перетащите файл на панель ЛОКАЛЬНЫЙ (слева)
- Отпустите в нужной папке

**Способ B: Правый клик**
- Щёлкните правой кнопкой на файле
- Выберите **«Скачать»**
- Файл скачивается в текущую локальную папку

### Загрузка файла (Локальный → Удалённый)

1. Перейдите к файлу на панели **ЛОКАЛЬНЫЙ** (слева)

**Способ A: Перетаскивание**
- Нажмите и перетащите файл на панель УДАЛЁННЫЙ (справа)
- Отпустите в нужной папке

**Способ B: Правый клик**
- Щёлкните правой кнопкой на файле
- Выберите **«Загрузить»**
- Файл загружается в текущую удалённую папку

### Создание новой папки

1. Перейдите в целевую директорию
2. Щёлкните правой кнопкой в пустом месте
3. Выберите **«Новая папка»**
4. Введите имя папки
5. Нажмите OK

### Создание нового файла

1. Перейдите в целевую директорию
2. Щёлкните правой кнопкой в пустом месте
3. Выберите **«Новый файл»**
4. Введите имя файла
5. Нажмите OK (создаётся пустой файл)

### Удаление файла или папки

1. Щёлкните правой кнопкой на файле или папке
2. Выберите **«Удалить»**
3. Подтвердите удаление

### Переименование файла или папки

1. Щёлкните правой кнопкой на файле или папке
2. Выберите **«Переименовать»**
3. Введите новое имя
4. Нажмите OK

## Изменение прав доступа (chmod)

1. Щёлкните правой кнопкой на файле или папке (панель УДАЛЁННЫЙ)
2. Выберите **«Изменить права доступа»**
3. Введите права в восьмеричном формате (например, `755`, `644`)
4. Нажмите OK

### Распространённые значения прав

| Значение | Смысл | Применение |
|----------|-------|------------|
| `755` | Владелец: полные, Другие: чтение+выполнение | Папки, исполняемые файлы |
| `644` | Владелец: чтение+запись, Другие: чтение | Обычные файлы |
| `600` | Только владелец | Приватные файлы, SSH-ключи |
| `777` | Все: полный доступ | Не рекомендуется |

## Редактирование удалённых файлов

1. Найдите текстовый файл на панели УДАЛЁННЫЙ
2. Дважды щёлкните на нём (или правый клик → Редактировать)
3. Файл откроется во встроенном редакторе
4. Внесите изменения
5. Нажмите **Сохранить** или `Ctrl+S`
6. Файл сохраняется обратно на сервер

### Поддерживаемые типы файлов

**Файлы кода:**
`.js`, `.ts`, `.py`, `.php`, `.java`, `.c`, `.cpp`, `.go`, `.rs`, `.rb`

**Конфигурационные файлы:**
`.json`, `.yml`, `.yaml`, `.toml`, `.ini`, `.conf`, `.env`

**Веб-файлы:**
`.html`, `.css`, `.scss`, `.vue`, `.svelte`

**Скрипты:**
`.sh`, `.bash`, `.ps1`, `.bat`

**Другие:**
`.txt`, `.md`, `.log`, `Dockerfile`, `Makefile`

## Сжатие файлов

Создание архивов из файлов или папок на удалённом сервере:

1. Щёлкните правой кнопкой на файле или папке (панель УДАЛЁННЫЙ)
2. Наведите на **«Сжать»**
3. Выберите формат:
   - **ZIP** — Универсальный, работает везде
   - **TAR.GZ** — Лучший для Linux/Unix
   - **TAR** — Архив без сжатия

Архив создаётся в той же директории.

## Извлечение архивов

Извлечение сжатых файлов на удалённом сервере:

1. Щёлкните правой кнопкой на файле архива (панель УДАЛЁННЫЙ)
2. Выберите **«Извлечь сюда»**
3. Файлы извлекаются в текущую директорию

### Поддерживаемые форматы архивов

| Формат | Расширения |
|--------|------------|
| ZIP | `.zip` |
| TAR | `.tar`, `.tar.gz`, `.tgz`, `.tar.bz2`, `.tar.xz` |
| GZIP | `.gz` |
| BZIP2 | `.bz2` |
| XZ | `.xz` |
| 7-Zip | `.7z` |
| RAR | `.rar` |

> **Примечание:** Для извлечения требуются соответствующие инструменты, установленные на сервере (unzip, tar, 7z, unrar).

## Свойства файла

Просмотр детальной информации о файлах и папках:

1. Щёлкните правой кнопкой на файле или папке
2. Выберите **«Свойства»**
3. Просмотрите детали:

| Свойство | Описание |
|----------|----------|
| Имя | Имя файла или папки |
| Путь | Полный путь |
| Размер | Размер файла в байтах/КБ/МБ/ГБ |
| Тип | Файл или Директория |
| Права | Чтение/запись/выполнение (например, 755) |
| Владелец | Пользователь-владелец (только удалённый) |
| Группа | Группа-владелец (только удалённый) |
| Изменён | Дата последнего изменения |
| Создан | Дата создания (если доступно) |
| Доступ | Дата последнего доступа |

## Очередь передачи

При передаче нескольких файлов:
- Полоса прогресса показывает общее завершение
- Отображается прогресс отдельных файлов
- Нажмите ✕ для отмены передачи
- Неудачные передачи можно повторить

## Горячие клавиши

| Сочетание | Действие |
|-----------|----------|
| `Ctrl+Shift+O` | Переключить Терминал/SFTP |
| `F2` | Переименовать выбранное |
| `Delete` | Удалить выбранное |
| `Ctrl+C` | Копировать путь |
| `Ctrl+R` | Обновить директорию |

## Советы

- **Перетаскивайте папки целиком** для рекурсивной передачи
- **Дважды щёлкните** на папке для входа
- Используйте **поиск/фильтр** для быстрого поиска файлов
- **Подтверждение перезаписи** предотвращает случайную потерю данных
