# Клиент баз данных

Marix включает встроенный клиент баз данных для подключения и управления серверами баз данных прямо из приложения.

## Интерфейс клиента баз данных

![Интерфейс клиента баз данных](/database.png)

---

## Поддерживаемые базы данных

| База данных | Протокол | Возможности |
|-------------|----------|-------------|
| **MySQL / MariaDB** | `mysql` | Редактор запросов, браузер таблиц, просмотр данных, инспектор структуры |
| **PostgreSQL** | `postgresql` | Редактор запросов, браузер таблиц, просмотр данных, инспектор структуры |
| **MongoDB** | `mongodb` | Браузер коллекций, просмотр документов, интерфейс запросов |
| **Redis** | `redis` | Браузер ключ-значение, командный интерфейс |
| **SQLite** | `sqlite` | Поддержка локальных файлов, полный SQL-редактор |

---

## Добавление сервера базы данных

1. Нажмите кнопку **+ Добавить** в боковой панели
2. Выберите вкладку **База данных** (или значок базы данных)
3. Выберите тип базы данных

### MySQL / MariaDB / PostgreSQL

| Поле | Описание | Пример |
|------|----------|--------|
| Имя | Отображаемое имя | `Production MySQL` |
| Хост | Имя хоста или IP сервера | `db.example.com` |
| Порт | Порт базы данных | `3306` (MySQL) / `5432` (PostgreSQL) |
| Имя пользователя | Пользователь БД | `root` |
| Пароль | Пароль пользователя | `••••••••` |
| База данных | База по умолчанию (опционально) | `myapp_production` |
| SSL | Включить SSL/TLS | ☑️ Включено |

### MongoDB

| Поле | Описание | Пример |
|------|----------|--------|
| Имя | Отображаемое имя | `MongoDB Atlas` |
| Строка подключения | Полный MongoDB URI | `mongodb+srv://user:pass@cluster.mongodb.net/` |
| Хост | Имя хоста сервера | `localhost` |
| Порт | Порт MongoDB | `27017` |
| База данных | База по умолчанию | `myapp` |

### Redis

| Поле | Описание | Пример |
|------|----------|--------|
| Имя | Отображаемое имя | `Redis Cache` |
| Хост | Имя хоста сервера | `redis.example.com` |
| Порт | Порт Redis | `6379` |
| Пароль | Пароль аутентификации (опционально) | `••••••••` |
| База данных | Номер базы данных | `0` |

### SQLite

| Поле | Описание | Пример |
|------|----------|--------|
| Имя | Отображаемое имя | `Local SQLite` |
| Путь к файлу | Путь к файлу .db | `/home/user/app.db` |

---

## Интерфейс клиента БД

### 📊 Вкладка «Данные»

Просмотр данных таблиц:

- **Выберите базу данных** из выпадающего списка (если несколько)
- **Нажмите на таблицу** в боковой панели для просмотра данных
- **Пагинация** — навигация по строкам (100 на страницу по умолчанию)
- **Сортировка по столбцам** — нажмите на заголовок столбца
- **Быстрый фильтр** — поиск среди отображаемых данных

### ⚡ Вкладка «Запрос»

Написание и выполнение SQL-запросов с:

- **Подсветка синтаксиса** — ключевые слова SQL, строки, числа
- **Выполнить** — запуск запроса с `Ctrl+Enter` или кнопкой Выполнить
- **Таблица результатов** — просмотр результатов запроса
- **Экспорт** — загрузка результатов в CSV или JSON
- **Время выполнения** — показывает длительность запросов

### 🏗️ Вкладка «Структура»

Просмотр схем таблиц:

- Имена столбцов и типы данных
- Первичные ключи, внешние ключи, уникальные ограничения
- Nullable-столбцы и значения по умолчанию
- Настройки автоинкремента

### 🔗 Вкладка «ERD»

Просмотр диаграммы сущность-связь:

- Визуальное представление таблиц и связей
- Связи внешних ключей показаны линиями
- Перетаскивание и масштабирование для навигации по большим схемам
- Экспорт в изображение (PNG)

---

## Горячие клавиши

| Сочетание | Действие |
|-----------|----------|
| `Ctrl/Cmd + Enter` | Выполнить запрос |
| `Ctrl/Cmd + S` | Сохранить запрос в файл |
| `Ctrl/Cmd + O` | Открыть запрос из файла |
| `Ctrl/Cmd + Shift + F` | Форматировать SQL |
| `Escape` | Отменить выполняемый запрос |

---

## Безопасность подключения

### Шифрование SSL/TLS

Включите флажок **SSL** при добавлении сервера. Для пользовательских сертификатов:

- Путь к CA-сертификату
- Путь к клиентскому сертификату (опционально)
- Путь к клиентскому ключу (опционально)

### SSH-туннель

Подключение через SSH-туннель:

1. Сначала добавьте SSH-сервер в Marix
2. При добавлении базы данных включите **SSH-туннель**
3. Выберите SSH-сервер для туннелирования

---

## Лучшие практики

1. **Используйте аккаунты только для чтения** — создавайте пользователей БД с ограниченными правами
2. **Ограничивайте результаты** — всегда используйте `LIMIT` чтобы не загружать слишком много данных
3. **Тестируйте перед выполнением** — для `UPDATE` или `DELETE` сначала выполните `SELECT`
4. **Используйте транзакции** — для критических операций оборачивайте в транзакции

---

## Устранение неполадок

| Проблема | Решение |
|----------|---------|
| Ошибка подключения | Проверьте хост/порт, учётные данные, файрвол, настройки SSL |
| Тайм-аут запроса | Добавьте LIMIT, проверьте индексы, проверьте нагрузку сервера |
| Доступ запрещён | Проверьте права доступа, права к базе данных, свяжитесь с администратором |
| Ошибка SSL-сертификата | Отключите SSL, проверьте срок действия сертификата, добавьте CA-сертификат |
