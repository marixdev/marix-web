# LAN-Dateiübertragung

Übertragen Sie Dateien zwischen Geräten im selben lokalen Netzwerk ohne Server.

## LAN-Dateiübertragungs-Oberfläche

![LAN-Dateiübertragungs-Oberfläche](/sendfile.png)

## Voraussetzungen

> ⚠️ **Wichtig:** Sowohl Sender als auch Empfänger müssen **LAN-Erkennung aktiviert** haben.
> 
> Klicken Sie auf die 🌐-Schaltfläche in der Kopfleiste oder verwenden Sie `Strg+Shift+L` zum Umschalten.

- Beide Geräte im selben Netzwerk (WLAN/LAN)
- LAN-Erkennung auf beiden Geräten aktiviert
- Marix läuft auf beiden Geräten

## LAN-Dateiübertragung öffnen

Klicken Sie auf **„Dateien senden"** in der Seitenleiste.

## Dateien senden

### Schritt 1: LAN-Erkennung aktivieren
Klicken Sie auf das **🌐**-Symbol (Globus) in der Kopfleiste.

### Schritt 2: Dateien auswählen
1. Gehen Sie zu Dateien senden → Registerkarte **Senden**
2. Klicken Sie auf **„Dateien auswählen"** oder **„Ordner auswählen"**
3. Ausgewählte Elemente erscheinen in der Liste
4. Klicken Sie auf × zum Entfernen eines Elements

### Schritt 3: Kopplungscode erhalten
Notieren Sie den angezeigten 6-stelligen **Kopplungscode**.

### Schritt 4: Code teilen
Teilen Sie dem Empfänger Ihren Kopplungscode mit (mündlich oder per Nachricht).

### Schritt 5: Senden starten
1. Klicken Sie auf **„Senden starten"**
2. Status: „Warte auf Empfänger..."
3. Die Übertragung beginnt, wenn der Empfänger sich verbindet

## Dateien empfangen

### Schritt 1: LAN-Erkennung aktivieren
Klicken Sie auf das **🌐**-Symbol (Globus) in der Kopfleiste.

### Schritt 2: Verbinden
1. Gehen Sie zu Dateien senden → Registerkarte **Empfangen**
2. Geben Sie den 6-stelligen Kopplungscode vom Sender ein
3. Klicken Sie auf **„Verbinden"**

### Schritt 3: Empfangen
- Geräte verbinden sich automatisch
- Die Übertragung beginnt sofort
- Dateien werden in Ihrem Downloads-Ordner gespeichert

## Übertragungsfortschritt

Während der Übertragung sehen Sie:
- Fortschrittsbalken mit Abschlussprozentsatz
- Geschwindigkeitsanzeige (z.B. „5,2 MB/s")
- Aktuell übertragene Datei

## Übertragung abbrechen

Klicken Sie auf die Schaltfläche **„Abbrechen"**, um die Übertragung jederzeit zu stoppen.

## Fehlerbehebung

### Sender/Empfänger nicht gefunden

1. Stellen Sie sicher, dass beide Geräte **LAN-Erkennung AN** haben (🌐-Symbol aktiv)
2. Beide Geräte müssen im **selben lokalen Netzwerk** sein
3. Prüfen Sie, ob die Firewall Marix nicht blockiert
4. Versuchen Sie, die Verbindung zu aktualisieren

### Übertragung fehlgeschlagen

- Große Dateien können Zeit brauchen - seien Sie geduldig
- Stellen Sie eine stabile Netzwerkverbindung sicher
- Versuchen Sie erneut zu senden mit einem neuen Kopplungscode
- Prüfen Sie den verfügbaren Speicherplatz beim Empfänger

### Langsame Übertragungsgeschwindigkeit

- Verwenden Sie kabelgebundenes Ethernet für schnellere Übertragungen
- Reduzieren Sie die Netzwerkauslastung
- Schließen Sie bandbreitenintensive Anwendungen
- Prüfen Sie die Router/Netzwerk-Leistung

## Sicherheit

- Übertragungen verwenden **Ende-zu-Ende-Verschlüsselung**
- 6-stelliger Kopplungscode stellt sicher, dass nur der beabsichtigte Empfänger Dateien erhält
- Keine Daten gehen über externe Server
- Dateien werden direkt zwischen Geräten übertragen

## Tipps

- **Große Ordner**: Wählen Sie den Ordner statt einzelner Dateien
- **Mehrere Dateien**: Sie können mehrere Elemente gleichzeitig auswählen
- **Empfang bestätigen**: Bestätigen Sie mit dem Empfänger, dass die Dateien korrekt angekommen sind
- **Netzwerkqualität**: Besseres Netzwerk = schnellere Übertragungen
