# WebSocket Client

เชื่อมต่อและทดสอบ WebSocket servers

## หน้าจอ WebSocket Client

![หน้าจอ WebSocket Client](/websocket.png)

## ภาพรวม

WebSocket Client ช่วยให้คุณเชื่อมต่อกับ WebSocket servers เพื่อทดสอบ, debug, และตรวจสอบการสื่อสารแบบ real-time

## เปิด WebSocket Client

คลิก **"WebSocket"** ในเมนูแถบด้านข้าง

## เชื่อมต่อกับ WebSocket Server

### ขั้นตอนที่ 1: ใส่ URL
ใส่ WebSocket URL ในช่อง URL:
```
ws://your-server.com:8080/path
wss://secure-server.com/path
```

### ขั้นตอนที่ 2: ตั้งค่า Headers (ไม่บังคับ)
คลิก **"Headers"** เพื่อเพิ่ม HTTP headers:
| Header | ค่า |
|--------|-------|
| Authorization | Bearer your-token |
| X-Custom-Header | custom-value |

### ขั้นตอนที่ 3: เชื่อมต่อ
คลิก **"เชื่อมต่อ"**

### ขั้นตอนที่ 4: ดูสถานะ
- 🟢 **เชื่อมต่อแล้ว** - พร้อมส่ง/รับข้อความ
- 🟡 **กำลังเชื่อมต่อ** - รอการเชื่อมต่อ
- 🔴 **ไม่ได้เชื่อมต่อ** - ปิดการเชื่อมต่อแล้ว

## ส่งข้อความ

### ส่งข้อความ Text
1. พิมพ์ข้อความในช่อง input
2. คลิก **"ส่ง"** หรือกด Enter

### ส่งข้อความ JSON
```json
{
  "type": "message",
  "data": {
    "text": "Hello, World!",
    "timestamp": 1234567890
  }
}
```

### ส่งข้อความ Binary
1. เลือก **"Binary"** mode
2. ใส่ข้อมูล hex หรือ base64
3. คลิก **"ส่ง"**

## ดูข้อความ

### Message Log
ข้อความที่ส่งและรับแสดงในพื้นที่ log:
- ⬆️ **ส่ง** - ข้อความที่คุณส่ง (สีเขียว)
- ⬇️ **รับ** - ข้อความจากเซิร์ฟเวอร์ (สีฟ้า)
- ⚠️ **Error** - ข้อผิดพลาด (สีแดง)
- ℹ️ **Info** - ข้อมูลระบบ (สีเทา)

### ตัวกรองข้อความ
- **ทั้งหมด** - แสดงทุกข้อความ
- **ส่ง** - แสดงเฉพาะที่ส่ง
- **รับ** - แสดงเฉพาะที่รับ
- **Errors** - แสดงเฉพาะข้อผิดพลาด

## ฟีเจอร์ขั้นสูง

### Auto-Reconnect
เปิดใช้งานเพื่อเชื่อมต่อใหม่อัตโนมัติเมื่อการเชื่อมต่อหลุด:
1. คลิก **"การตั้งค่า"**
2. เปิดใช้ **"Auto-Reconnect"**
3. ตั้งค่า delay (วินาที)

### Ping/Pong
ส่ง ping เพื่อตรวจสอบการเชื่อมต่อ:
1. คลิกปุ่ม **"Ping"**
2. ดู pong response ใน log

### ข้อความที่บันทึกไว้
บันทึกข้อความที่ใช้บ่อย:
1. พิมพ์ข้อความ
2. คลิก **"บันทึก"** (ไอคอนดาว)
3. ตั้งชื่อ
4. เรียกใช้จากรายการ saved messages

## จัดการการเชื่อมต่อ

### บันทึกการเชื่อมต่อ
1. หลังจากเชื่อมต่อสำเร็จ
2. คลิก **"บันทึกการเชื่อมต่อ"**
3. ตั้งชื่อ
4. เรียกใช้ในภายหลัง

### ดูการเชื่อมต่อที่บันทึก
1. คลิก **"การเชื่อมต่อที่บันทึก"**
2. เลือกการเชื่อมต่อ
3. คลิก **"เชื่อมต่อ"**

### ลบการเชื่อมต่อ
1. ค้นหาการเชื่อมต่อในรายการ
2. คลิกไอคอน **ถังขยะ**
3. ยืนยันการลบ

## Export Logs

1. คลิก **"Export"**
2. เลือก format:
   - **JSON** - สำหรับการวิเคราะห์
   - **Text** - สำหรับการอ่าน
3. บันทึกไฟล์

## ตัวอย่างการใช้งาน

### ทดสอบ Chat Server
```
URL: wss://chat-server.com/ws
Message: {"type":"join","room":"general"}
```

### ทดสอบ Real-time Updates
```
URL: wss://api.example.com/live
Message: {"subscribe":"ticker"}
```

### ทดสอบ Echo Server
```
URL: wss://echo.websocket.org
Message: Hello!
```

## แก้ปัญหา

### ไม่สามารถเชื่อมต่อ
- ตรวจสอบ URL ถูกต้อง (ws:// หรือ wss://)
- ยืนยันว่าเซิร์ฟเวอร์กำลังทำงาน
- ตรวจสอบ firewall
- ลอง protocol อื่น (ws ↔ wss)

### การเชื่อมต่อหลุดทันที
- ตรวจสอบ authentication headers
- ยืนยัน origin ที่อนุญาต
- ตรวจสอบ SSL certificate (สำหรับ wss)

### ข้อความไม่ถูกส่ง
- ยืนยันว่าเชื่อมต่ออยู่
- ตรวจสอบ format ข้อความ
- ดู error logs

## เคล็ดลับ

1. **ใช้ wss** สำหรับการเชื่อมต่อที่ปลอดภัย
2. **บันทึกการเชื่อมต่อ** ที่ใช้บ่อย
3. **ใช้ JSON formatting** เพื่ออ่านง่าย
4. **Export logs** สำหรับการ debug
5. **เปิด Auto-Reconnect** สำหรับการทดสอบยาวนาน
