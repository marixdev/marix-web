# ไคลเอนต์ฐานข้อมูล

Marix มีไคลเอนต์ฐานข้อมูลในตัวสำหรับเชื่อมต่อและจัดการเซิร์ฟเวอร์ฐานข้อมูลโดยตรงจากแอปพลิเคชัน

## หน้าจอไคลเอนต์ฐานข้อมูล

![หน้าจอไคลเอนต์ฐานข้อมูล](/database.png)

---

## ฐานข้อมูลที่รองรับ

| ฐานข้อมูล | โปรโตคอล | ฟีเจอร์ |
|----------|----------|----------|
| **MySQL / MariaDB** | `mysql` | ตัวแก้ไข query, เรียกดูตาราง, ดูข้อมูล, ตรวจสอบโครงสร้าง |
| **PostgreSQL** | `postgresql` | ตัวแก้ไข query, เรียกดูตาราง, ดูข้อมูล, ตรวจสอบโครงสร้าง |
| **MongoDB** | `mongodb` | เรียกดู collection, ดูเอกสาร, อินเทอร์เฟซ query |
| **Redis** | `redis` | เรียกดู key-value, อินเทอร์เฟซคำสั่ง |
| **SQLite** | `sqlite` | รองรับไฟล์ในเครื่อง, ตัวแก้ไข SQL เต็มรูปแบบ |

---

## เพิ่มเซิร์ฟเวอร์ฐานข้อมูล

1. คลิกปุ่ม **+ เพิ่ม** ในแถบด้านข้าง
2. เลือกแท็บ **ฐานข้อมูล** (หรือไอคอนฐานข้อมูล)
3. เลือกประเภทฐานข้อมูลของคุณ

### MySQL / MariaDB / PostgreSQL

| ฟิลด์ | คำอธิบาย | ตัวอย่าง |
|-------|-------------|---------|
| ชื่อ | ชื่อที่แสดง | `Production MySQL` |
| Host | hostname หรือ IP ของเซิร์ฟเวอร์ | `db.example.com` |
| Port | พอร์ตฐานข้อมูล | `3306` (MySQL) / `5432` (PostgreSQL) |
| Username | ผู้ใช้ฐานข้อมูล | `root` |
| Password | รหัสผ่านผู้ใช้ | `••••••••` |
| Database | ฐานข้อมูลเริ่มต้น (ไม่บังคับ) | `myapp_production` |
| SSL | เปิดใช้งาน SSL/TLS | ☑️ เปิดใช้งาน |

### MongoDB

| ฟิลด์ | คำอธิบาย | ตัวอย่าง |
|-------|-------------|---------|
| ชื่อ | ชื่อที่แสดง | `MongoDB Atlas` |
| Connection String | MongoDB URI เต็ม | `mongodb+srv://user:pass@cluster.mongodb.net/` |

### Redis

| ฟิลด์ | คำอธิบาย | ตัวอย่าง |
|-------|-------------|---------|
| ชื่อ | ชื่อที่แสดง | `Redis Cache` |
| Host | hostname ของเซิร์ฟเวอร์ | `redis.example.com` |
| Port | พอร์ต Redis | `6379` |
| Password | รหัสผ่าน Auth (ไม่บังคับ) | `••••••••` |

### SQLite

| ฟิลด์ | คำอธิบาย | ตัวอย่าง |
|-------|-------------|---------|
| ชื่อ | ชื่อที่แสดง | `Local SQLite` |
| File Path | เส้นทางไปยังไฟล์ .db | `/home/user/app.db` |

---

## อินเทอร์เฟซไคลเอนต์ฐานข้อมูล

### 📊 แท็บข้อมูล

เรียกดูและดูข้อมูลตาราง:

- **เลือกฐานข้อมูล** จากเมนูดร็อปดาวน์
- **คลิกตาราง** ในแถบด้านข้างเพื่อดูข้อมูล
- **การแบ่งหน้า** - เลื่อนดูแถว

### ⚡ แท็บ Query

เขียนและทำ SQL query ด้วย:

- **Syntax highlighting** - คำหลัก SQL, strings, ตัวเลข
- **Execute** - รัน query ด้วย `Ctrl+Enter`

### 🏗️ แท็บโครงสร้าง

ตรวจสอบ schema ตาราง

### 🔗 แท็บ ERD

ดูแผนภาพ Entity-Relationship

---

## แป้นพิมพ์ลัด

| แป้นพิมพ์ลัด | การทำงาน |
|----------|--------|
| `Ctrl/Cmd + Enter` | ทำ query |
| `Ctrl/Cmd + S` | บันทึก query เป็นไฟล์ |
| `Escape` | ยกเลิก query ที่กำลังรัน |

---

## การแก้ไขปัญหา

| ปัญหา | วิธีแก้ไข |
|-------|----------|
| การเชื่อมต่อล้มเหลว | ตรวจสอบ host/port, ข้อมูลประจำตัว, firewall |
| Query หมดเวลา | เพิ่ม LIMIT, ตรวจสอบ index |
| Permission Denied | ตรวจสอบ grants, การเข้าถึงฐานข้อมูล |
